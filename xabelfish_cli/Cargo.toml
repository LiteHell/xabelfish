[package]
name = "xabelfish_cli"
version = "0.1.0"
edition = "2024"

[dependencies]
unix_socket_transport = { version = "0.1.0", path = "../unix_socket_transport" }
xabelfish_socket_protocol = { version = "0.1.0", path = "../xabelfish_socket_protocol" }
